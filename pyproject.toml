[project]
name = "dashbls"
description = "Python 3.x binds for Dash's bls-signatures"
authors = [{ name = "Dash Core Developers" }]
keywords = ["bls", "signatures", "bls12-381", "dash", "cryptography"]
license = "Apache-2.0"
license-files = ["LICENSE"]
readme = "README.md"
requires-python = ">=3.10"
dynamic = ["version"]

[project.urls]
Homepage = "https://github.com/dashpay/bls-signatures"
Source = "https://github.com/dashpay/bls-signatures"
Issues = "https://github.com/dashpay/bls-signatures/issues"

[project.optional-dependencies]
dev = [
  "ruff>=0.9",
  "pytest>=8.1",
  "pytest-benchmark>=5.1",
]

[build-system]
requires = ["setuptools>=77", "setuptools_scm>=8", "pybind11>=2.13.6"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
local_scheme = "no-local-version"

[tool.pytest.ini_options]
testpaths = ["binds/python"]
addopts = "--benchmark-skip"

[tool.ruff]
indent-width = 4
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = [
  "ANN", # flake8-annotations
  "B",   # flake8-bugbear
  "BLE", # flake8-blind-except
  "E",   # pycodestyle errors
  "F",   # pyflakes
  "I",   # isort
  "RUF", # ruff
  "S",   # flake8-bandit
  "TCH", # flake8-type-checking
  "UP",  # pyupgrade
  "W",   # pycodestyle warnings
]

[tool.ruff.lint.per-file-ignores]
"binds/python/test*.py" = ["S101"]
"binds/python/samples/*.py" = ["S101"]

[tool.ruff.format]
indent-style = "space"
line-ending = "lf"
quote-style = "double"
skip-magic-trailing-comma = false
